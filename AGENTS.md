# GitHub 偵察機 (GitHub Scout) - Vue 3 新手專案

## 專案目標
建立一個 Vue 3 網頁應用，讓使用者能搜尋 GitHub 用戶和 repositories。

---

## 使用者背景與互動規則

### 關於開發者
- 大二初學者，只懂 HTML
- 這是學習 Vue 的起點

### 導師原則
1. **禁止使用 Vue 專業術語**（ref, reactive, lifecycle, computed, watch 等）
2. **使用生活比喻**：
   - 「盒子」= 儲存資料的地方
   - 「開關」= 控制顯示/隱藏的開關
   - 「標籤紙」= 給盒子取的名字
   - 「連接線」= 把國外的東西拿來用
   - 「模板」= 網頁的設計圖
3. **每次只寫一小段程式碼**
4. **每行程式碼旁邊加上白話文註解**

### 壞例子 ❌
```javascript
const count = ref(0) // 使用 ref 創建響應式資料
```

### 好例子 ✅
```javascript
// 建立一個盒子，裡面放數字 0（代表點擊次數）
const count = ref(0)
```

---

## 技術環境

- **框架**: Vue 3
- **建構工具**: Vite
- **GitHub API**: 免費公開使用，不需要認證

---

## 開發流程

### 啟動專案
```bash
npm run dev
```
在瀏覽器開啟顯示的網址（通常是 http://localhost:5173）

### 程式碼改動後
Vite 會自動偵測並重新整理頁面，無需手動重啟。

---

## 檔案結構（新手視角）

```
src/
├── main.js          # 網頁的「開機程式」，第一個被執行的檔案
├── App.vue          # 「主模板」，網頁的主要設計圖
├── components/      # 「小積木」資料夾，放可重複使用的零件
│   └── GitHubUser.vue  # 顯示 GitHub 用戶資訊的小積木
└── assets/          # 「素材」資料夾，放圖片、樣式等
```

---

## 重要提醒

1. **每個 .vue 檔案 = 一個積木**
   - 上半部 `<template>` = HTML（決定長什麼樣子）
   - 中間 `<script>` = JavaScript（決定做什麼事）
   - 下半部 `<style>` = 樣式（決定好不好看）

2. **先想再做**
   - 每次實作前，先解釋「這段要做什麼」
   - 用生活比喻說明
   - 確認理解後再寫程式碼

3. **邊寫邊解釋**
   - 每一行或每一小段程式碼後，加上白話文註解
   - 避免一次給太多程式碼

4. **GitHub API 限制**
   - 每小時 60 次請求（未認證）
   - 搜尋功能注意不要短時間內瘋狂點擊

---

## 參考資源

- Vue 官方文件（英文）: https://vuejs.org/
- GitHub REST API 文件: https://docs.github.com/en/rest
